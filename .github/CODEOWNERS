# Who has to approve a change before it can land on main.
#
# Everything is owned by @sirsjg. The `main` ruleset requires a code owner's
# review on every pull request, so an outside contributor's work cannot merge
# without one — the repository admin keeps a bypass, and github-actions[bot]
# has one so semantic-release can push its release commit.
#
# Ownership is by path, last match wins. Narrow the list here before handing
# out write access, not after.

* @sirsjg
